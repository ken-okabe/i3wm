 

i squashfs-tool

 sudo unsquashfs airootfs.sfs

mount /dev/sda1  /mnt

nano /etc/resolv.conf
edit
nameserver 8.8.8.8

nano /etc/pacman.d/mirrorlist
## Japan
Server = http://ftp.jaist.ac.jp/pub/Linux/ArchLinux/$repo/os/$arch


pacstrap /arch0 base base-devel
genfstab -U -p /mnt >> /mnt/etc/fstab

poweroff then new installed /dev/sda3
sudo mount /dev/sda1 /mnt

#target-system nspawn

pacman -R linux
pacman -S sudo fish linux-lts wget git htop xf86-video-intel noto-fonts-cjk bluez bluez-utils unzip ark fcitx-im fcitx-configtool fcitx-mozc atom ranger nodejs npm openssh mpv mcomix transmission-qt dunst parcellite pavucontrol volumeicon network-manager-applet  xorg-server xorg-xinit i3 networkmanager gparted qt5ct dolphin  xorg-xbacklight  pulseaudio lxterminal leafpad lxappearance breeze-gtk breeze-icons blueman


export EDITOR=nano
visudo
=====th end of lines========
ken ALL=NOPASSWD: ALL

nano /etc/pacman.conf

[archlinuxfr]
SigLevel = Never
Server = http://repo.archlinux.fr/$arch

pacman -S --refresh yaourt

nano /etc/locale.gen
---
en_US.UTF-8 UTF-8
ja_JP.UTF-8 UTF-8

locale-gen

echo LANG=en_US.UTF-8 > /etc/locale.conf ; 
export LANG=en_US.UTF-8
 
systemctl enable NetworkManager.service
systemctl enable bluetooth
 
timedatectl set-timezone Asia/Tokyo

useradd -m -g wheel nano   ken
passwd ken

chsh ken
/usr/bin/fish

exit
log in ken

cp /etc/X11/xinit/xinitrc ~/.xinitrc
nano .xinitrc

export QT_QPA_PLATFORMTHEME=qt5ct; \
i3;


yaourt -S j4-dmenu-desktop google-chrome

ken@localhost ~> .config/
ken@localhost ~/.config> mkdir i3status
ken@localhost ~/.config>  cp /etc/i3status.conf ~/.config/i3status/config

sudo poweroff


cd /
┌[ken@localhost]-(/)
└> sudo tar -cpf /arch0.tar arch0 --totals




┌[ken@localhost]-(~)
└> sudo mkdir /usb
┌[ken@localhost]-(~)
└>  sudo mount /dev/sdc1 /usb
┌[ken@localhost]-(~)
└> sudo dd if=/arch0.tar of=/usb/arch0.tar bs=1M




sudo mount /dev/sda1 /mnt
cd /mnt/
sudo tar -xpf /arch0.tar --strip=1 --totals










